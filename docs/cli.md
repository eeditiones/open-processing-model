# CLI Reference

The `opm` command is a [Typer](https://typer.tiangolo.com/) application. The
reference below is generated from the command definitions in `opm.cli`. Run
`uv run opm --help` (or `opm <command> --help`) for the same information in your
terminal.

::: mkdocs-typer2
    :module: opm.cli
    :name: opm
