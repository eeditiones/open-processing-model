# typst_compile

Compile Typst output to PDF with the `typst` command.
Get the Typst source from [`Project.transform`](project.md) with `mode='typst'`,
then pass it to `compile_pdf`.

::: opm.typst_compile
