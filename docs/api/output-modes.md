# output modes

Everything that differs between output modes, in one table. Adding a mode, or
changing which models it takes, which output functions render it or which
template wraps it, happens here.

::: opm.output_modes
